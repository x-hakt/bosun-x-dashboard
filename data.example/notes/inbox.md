# Inbox

Freeform scratch space for ideas and todos that aren't tied to a specific project yet.

- Try swapping the reverse proxy for Caddy on the next rebuild
- The NAS SMB mount drops after a power cut — add a systemd automount
- Look at whether the VPS actually needs 2 vCPU or if 1 is fine
