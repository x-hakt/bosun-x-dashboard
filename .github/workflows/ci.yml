name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      # the bosun-x devDependency is a github: URL; the lockfile resolves it over ssh
      - run: git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"
      - run: npm ci
      # build first: `next build` type-checks with the route types it generates,
      # which a bare `tsc` can't see in a clean checkout (LayoutProps et al.)
      - run: npm run build
      - run: npm run lint
      - run: npx tsc --noEmit
